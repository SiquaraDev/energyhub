# Config NÃO sensível do audit-service (Fase 16).
apiVersion: v1
kind: ConfigMap
metadata:
  name: audit-service-config
  namespace: energyhub
  labels:
    app: audit-service
    app.kubernetes.io/part-of: energyhub
data:
  APP_NAME: "audit-service"
  APP_PORT: "8005"
  SERVICE_HOST: "audit-service"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
