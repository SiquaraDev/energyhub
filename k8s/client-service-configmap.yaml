# Config NÃO sensível do client-service (Fase 16).
apiVersion: v1
kind: ConfigMap
metadata:
  name: client-service-config
  namespace: energyhub
  labels:
    app: client-service
    app.kubernetes.io/part-of: energyhub
data:
  APP_NAME: "client-service"
  APP_PORT: "8002"
  SERVICE_HOST: "client-service"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
